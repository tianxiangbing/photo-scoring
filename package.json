{
  "name": "photo-scoring",
  "version": "0.1.0",
  "private": true,
  "author": "tianxiangbing",
  "dependencies": {
    "html2canvas": "^0.5.0-beta4",
    "react": "15.4.1",
    "react-addons-perf": "^15.4.2",
    "react-dom": "15.4.1",
    "react-perf": "^1.0.1",
    "react-redux": "^5.0.6",
    "redux": "^3.7.2"
  },
  "devDependencies": {
    "react-scripts": "1.0.16"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "docs": "cd build && git init && git checkout -b gh-pages && git add --all && git commit -am \"update page\" && git push git@github.com:tianxiangbing/test gh-pages --force"
  }
}
